<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dnieln7.testing">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TestingExamplesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestingExamples">
        <activity
            android:name=".ui.hilt.spacex.SpacexActivity"
            android:exported="false" />
        <activity
            android:name=".ui.work.WorkManagerActivity"
            android:exported="false" />
        <activity
            android:name=".ui.room.RoomActivity"
            android:exported="false" />
        <activity
            android:name=".ui.retrofit.RetrofitActivity"
            android:exported="false" />
        <activity
            android:name=".ui.navigation.NavigationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>